# apiVersion: external-secrets.io/v1
# kind: ExternalSecret
# metadata:
#   name: kargo-bitrbucket-repo-creds
# spec:
#   refreshInterval: "4h"
#   secretStoreRef:
#     kind: SecretStore
#     name: "{{.Release.Namespace}}-backend"
#   target:
#     name: kargo-bitrbucket-repo-creds
#     creationPolicy: Owner
#     template:
#       metadata:
#         labels: 
#           kargo.akuity.io/cred-type: git
#       data:
#         repoURL: "https://bitbucket.org.*"
#         username: "{{ `{{ .username }}` }}"
#         password: "{{ `{{ .password }}` }}"
#         repoURLIsRegex: "true"
#   data:
#     - remoteRef:
#         key: bitbucket 
#         property: username
#       secretKey: username
#     - remoteRef:
#         key: bitbucket 
#         property: password
#       secretKey: password

    