gitea:
  ingress:
    enabled: true
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: websecure
      cert-manager.io/cluster-issuer: selfsigned
    hosts:
      - host: vcs.localhost.nip.io
    tls:
      - secretName: gitea-server-tls
        hosts:
          - vcs.localhost.nip.io
  valkey-cluster:
    enabled: false
  valkey:
    enabled: true
  postgresql-ha:
    enabled: false
  postgresql:
    enabled: true